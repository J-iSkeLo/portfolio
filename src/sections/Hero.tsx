import React from 'react'

interface Props {
}

const Hero: React.FC<Props> = (props) => {
    return (
        <header className="bg-hero min-h-[400px]">
        </header>
    )
}

export default Hero